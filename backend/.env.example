# Source Directories
# Base directory containing source files to process
# The system will scan subdirectories: forms/, emails/, invoices/
BASE_DIR=dummy_data

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Google Sheets Configuration
# Google Sheets spreadsheet ID where approved data will be written
# Find this in your spreadsheet URL: https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
GOOGLE_SPREADSHEET_ID=your-spreadsheet-id-here

# Redis Configuration
# Redis connection URL for PubSub and real-time events
# Format: redis://[username]:[password]@[host]:[port]/[db]
# For Docker Compose: redis://:techflow_redis_password@redis:6379/0
# For local Redis: redis://localhost:6379/0
# For Redis Cloud: redis://default:password@host:port/0
REDIS_URL=redis://password@host:port/db

# Logging
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# API Configuration
# Allowed CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
